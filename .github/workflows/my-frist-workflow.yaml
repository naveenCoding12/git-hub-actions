name: first-workflow

on: [push]

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: welcome message
        run: echo "my first GitHub Actions"
        
      - name: checkout
        uses: actions/checkout@v4

      - name: list files
        run: ls

      - name: read the file
        run: cat README.md
